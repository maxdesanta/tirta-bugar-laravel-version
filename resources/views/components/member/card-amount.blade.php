<div class="card-member-amount container">
    <div class="image-amount container">
        <img src="{{$image}}" alt="{{$title}}">
    </div>
    <div class="title-amount-group">
        <h3>{{$title}}</h3>
        <h4 id="total-member">{{$amount}}</h4>
    </div>
</div>